port: 8080 # Port of the service
params: # Empty if no params
audio: "test.wav" # path to the audio file
request_type: "audio" # type of request, select one from 'text', 'structuredtext', 'audio'
response_type: "annotations" # type of response, select one from 'annotations', 'audio', 'classification', 'texts'
